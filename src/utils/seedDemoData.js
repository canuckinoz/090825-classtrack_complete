// This is a mock utility file.
// In a real application, this would contain logic to generate demo data.

export const getConstellationEffects = (studentId) => {
  // This is a mock implementation.
  // In a real application, this would generate constellation effects based on student data.
  return {
    starBrightness: 0.8,
    pulseEffect: false,
    shouldShowShootingStar: false,
    trailColor: '#FFD700'
  };
};
